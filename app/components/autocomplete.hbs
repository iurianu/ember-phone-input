<AuInput
  list={{this.listId}}
  value={{@value}}
  ...attributes
  onInput={{perform this.onInputTask}}
/>
<br>
<datalist id={{this.listId}}>
  {{#each @options as |value|}}
    <option value={{value}} />
  {{/each}}
</datalist>