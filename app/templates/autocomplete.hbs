{{page-title "Autocomplete"}}

<AuLabel for="search-input">
  Search GitHub Repositories
</AuLabel>
<br>
<br>
<Autocomplete
  id="search-input"
  placeholder="Example: ember"
  @value={{this.repo}}
  @onInput={{this.setRepo}}
  @search={{this.searchRepos}}
  @options={{this.repos}}
/>